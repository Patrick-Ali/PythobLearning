import sys


Message1 = sys.stdin.read() 

print (Message1)

Message2 = input('Input message here: ')

# sys.stdin.read() allows you to enter multiple lines of information
# rather than the one long line that the input() function allows.
